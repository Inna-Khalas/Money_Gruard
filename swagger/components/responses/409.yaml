description: Conflict - User already exists
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - error
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: Something went wrong
        error:
          type: string
          example: Email in use
